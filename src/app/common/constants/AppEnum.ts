export enum TeamBoardType {
  TEAM = 'team',
  PROFMED = 'profmed',
}

export enum FileType {
  PHOTO = 'photo',
  LICENCE = 'licence',
  RESUME = 'resume',
}

export enum StatusCode {
  NOT_FOUND = 404,
  EXPIRED = 402,
  OK = 200,
}

export enum AssignmentStatus {
  PENDING = 'pending',
  ACTIVE = 'active',
  DFD = 'dfd',
  COMPLETED = 'completed',
}
